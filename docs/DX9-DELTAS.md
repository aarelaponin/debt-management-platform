# DX9-DELTAS — living compatibility log (workflow §7)

Generator skills are DX8-validated; jdx9 (9.0.7) is the target. Every Stage-3 batch
records import findings here; fixes go to the skill/generator, never the output.

| Date | Batch | Artefact class | Finding | Resolution |
|---|---|---|---|---|
| 2026-06-11 | CMBB-F01 | userview | Theme reference (Dx8TrimedaTheme shapes) not available in skill cache; generated with empty theme (Joget default applied on import) | Set theme via UI after first import OR capture DX9 theme JSON from jdx9 export and add to gen_userview.py — pending first import |
| 2026-06-12 | CMBB-F01 | app import | DX8 JSON endpoints `/web/json/console/app/import` and `/app/submit` REMOVED in 9.0.7 (404) | Use HTML console: POST multipart to `/web/console/app/import/submit` with `appZip` + `OWASP_CSRFTOKEN` (from `/csrf`, Referer header required) — encoded in `scripts/deploy_jwa.py` |
| 2026-06-12 | CMBB-F01 | forms/datalists/userview | Imported via JWA cleanly: 18+18+1 definitions verified in app_form/app_datalist/app_userview; userview renders | none — generators DX9-clean for these artefact classes (first evidence) |
| 2026-06-12 | CMBB-F01 | userview access | Userview rendered without login (no permission set) | DEV acceptable; set LoggedInUserPermission at F02 or via console |
| 2026-06-12 | CMBB-F01 | API Builder data path | `api_credential` row with blank `apiid` is NOT a wildcard in 9.0.7 — 401 until key bound to the API id | Bind key to its API (`apiid='API-<app>-data'`); encoded in deploy docs |
| 2026-06-12 | CMBB-F01 | re-import | Importing over an EXISTING app creates a new version in Git only (no DB materialization; API defs invisible to runtime) | DEV update cycle: **delete app → import → publish** (app_fd_* data survives app deletion); revisit for INT promotion |
| 2026-06-12 | CMBB-F02 | workflow/XPDL deploy | Shark "Failed to allocate object id" → RepositoryPersistenceManager null → "Error deploying package": schema-only clone of shk* tables left `objectid` EMPTY | Seed `INSERT INTO objectid (nextoid) VALUES (1000000)` + full Tomcat restart (PackageAdmin is a boot-time singleton). Added to bootstrap notes |
| 2026-06-12 | CMBB-F02 | tomcat.sh restart | `restart` without CATALINA_PID does not stop the old JVM — "restart" silently no-ops | kill PID explicitly, then start; verify a NEW "Server startup in" log line |
| 2026-06-12 | CMBB-F02 | /web/json/* APIs | JsonResponseFilter rejects POST/GET without same-origin Referer ("Possible CSRF attack") → HTTP 400 | Send `Referer: <base>/web/console/home` on every JSON API call (plus OWASP_CSRFTOKEN on POSTs) |
| 2026-06-12 | CMBB-F02 | participant map | `type=role` supports ONLY adminUser/loggedInUser (AppWorkflowHelper); org roles as role→silently empty whitelist → process start no-ops with empty processId | Org roles are directory GROUPS: `type=group` + dir_group rows (cmbb_user, dm_supervisor seeded, admin member). Spec + gen updated |
| 2026-06-12 | CMBB-F02 | Hibernate cache vs SQL | SQL DELETE on app_fd_* rows poisons Hibernate entity cache → later saveOrUpdate of same id = persistent 500 StaleObjectState until restart | Never SQL-delete rows that will be re-upserted; test fixtures use per-run unique ids; config rows only ever API-upserted |
| 2026-06-12 | CMBB-F02 | cmEvent hash chain | dateCreated is second-granular: consecutive guard events tie → lastEventHash picked wrong row → chain forked (caught by T-02.3) | CaseEventWriter sorts on ms-precision custom field eventTime; fix shipped in cmbb-plugins JAR |
| 2026-06-12 | CMBB-F03 | form postProcessor | Post Form Submission Processing (enterprise) FIRES on the AppFormAPI /saveOrUpdate path too — cmReassign REASSIGN ran end-to-end via API | Positive finding; postProcessor is a reliable P3-clean trigger for form-driven engine actions. gen_forms.py emits postProcessor from spec |
| 2026-06-12 | CMBB-F04 | DatePicker | `currentDateAs:"today"` in generated DatePicker acts as a MAX-date validation ("Value need to be before or on <today>") — future dates silently rejected on the API path (HTTP 200 + errors body) | gen_forms.py emits `currentDateAs:""`; API loaders must check the response BODY for `errors`, not just status 200 |
| 2026-06-12 | CMBB-F05 | PluginWebSupport | 9.0.7's `webService()` signature is **jakarta.servlet** (wflow-plugin-base-9.0.7.jar) — plugins compiled against wflow-core 8.1 (javax) would implement the WRONG method and never be invoked | Avoid PluginWebSupport while building against 8.1; form postProcessor is the working trigger alternative (sweep = auditable cmSweepRun row). Revisit when building against 9.x artefacts |
| 2026-06-12 | CMBB-F06 | FormDataDao reads | find/count on a not-yet-created app_fd_* table raises SQL error that marks the JTA tx rollback-only — try/catch does NOT recover; whole request 500s | Plugins must `updateSchema(formId, table, empty FormRowSet)` for owned tables before first read; cross-feature reads (cmHold pre-F08) gated behind a flag, not try/catch |
| 2026-06-12 | CMBB-F07 | OSGi classpath | wflow-core's bundled com.lowagie (iText) compiles against the provided jar but is NOT exported to the OSGi container — ClassNotFoundException at runtime despite DynamicImport-Package * | Embed the library in the plugin bundle (openpdf 1.3.30, compile scope + Embed-Dependency); rule: never rely on webapp-internal packages at plugin runtime |
| 2026-06-12 | CMBB-F03/F07 | datalist date formatter | `org.joget.apps.datalist.lib.DateFormatter` does not exist — App Composer shows "plugins not installed" banner (user-spotted) | Correct class is `org.joget.plugin.enterprise.DateFormatter` with property `dataFormat` (GAM-verified); gen_datalists.py fixed, datalist regenerated |
| 2026-06-12 | CMBB-F07 | cmEvent chain ordering | Same-MILLISECOND eventTime ties across different CaseEventWriter instances (guard vs engines in one MultiTools chain) forked the hash chain — regression T-02.3 caught it intermittently | cmEvent gains zero-padded `seq`; writers continue from max(seq); chain queries/verifications order by seq (F09's chain-completion scope, pulled forward) |
| 2026-06-12 | CMBB-F08 | seed id (multi-type) | `load_md_seed.row_id` derives an mmState id from `code` (NEW/OPEN/CLOSED) — fine with one case type, but a SECOND type (TESTD) collides on the same ids and would overwrite TEST's states | F08 seed CSVs carry an explicit `id` column (TESTD-NEW, …); rule: any seed adding a second case type's states/transitions MUST set explicit unique ids |
| 2026-06-12 | CMBB-F08 | closure decision gate | `requireDecision` cannot be the TransitionGuard plugin property: the single cmCaseEnvelope process has ONE guardFinal with ONE property value, so a global `true` would force decisions on every type and break the F02–F07 TEST regression | Per-type flag `mmCaseType.requireDecision` (F01 form amended); ClosePhase reads the type row; plugin property kept as a global override only. Test gate uses a dedicated TESTD type |
| 2026-06-12 | CMBB-F08 | plugin budget | Decisions/linkage/pending-info are form-driven actions needing a P3-clean trigger, but the CAD budgeted "no plugin" for decisions (assumed Joget native multi-stage approval, which the no-new-XPDL convention forbids) | Added **DecisionEngine** (post-processor, modes DECIDE/LINK/INFO_REQUEST/INFO_RESPONSE) alongside the budgeted HoldConnector; recorded as CAD amendment (budget 9→11) |
| 2026-06-12 | CMBB-F08 | deploy: new forms not materialised | `deploy_jwa.py` (import-over-existing) created a new Git version but the SIX new F08 forms never appeared in `app_form` → every post to them returned **HTTP 400** (form not found), while pre-existing forms still worked. Confirms the F01 note: import-over-existing does not materialise NEW definitions | DEV cycle MUST be **delete → import → publish**. Added `scripts/delete_app.py`; `deploy_jwa.py` now deletes first (`--keep` to skip). `app_fd_*` data survives the delete |
| 2026-06-12 | CMBB-F08 | deploy: API defs need restart | Immediately after a fresh delete→import→publish, ALL form-API posts (even existing forms) returned **HTTP 400 "Bad Request"** until Tomcat was restarted — the API Builder definitions are loaded at boot, like PackageAdmin (F02). 401 = key/binding problem; **400 right after a reimport = API defs not yet live, restart Tomcat** | Restart Tomcat (kill PID → start) after the deploy and BEFORE seeding; `deploy_jwa.py` prints this reminder. Then the same api_credential key works unchanged |
| 2026-06-12 | CMBB-F09 | deploy: hardcoded version 1 | `deploy_jwa.py` deleted+published `/{app}/1/...`, but delete→import advances the app **version** (we reached v5). Deleting/publishing v1 silently no-ops → **NOTHING published** (`app_app.published='f'` for every version) → `ApiBuilder.getJson` does `iterator().next()` for the def whose `appVersion == getPublishedVersion()`, finds none → **NoSuchElementException → HTTP 400 on EVERY form post** (looks identical to the F08 "needs restart" 400, but a restart does NOT fix it). Stale versions also accumulate: `app_datalist` returns >1 row per id → test helpers reading the deployed def (`deployed_sql`, run_t04/05) get concatenated JSON ("Extra data") | Discover real versions via psql and **publish the version actually created**, **delete all prior versions** (clean single-version DEV state). Fixed in `deploy_jwa.py`; one-off helpers `scripts/_publish_v.py <app> <ver>` and `_delete_v.py <app> <ver…>`. Diagnose: `SELECT appversion,published FROM app_app WHERE appid=…` |
| 2026-06-12 | CMBB-F09 | plugin property defaults | A plugin-property JSON `"value"` default is **UI-only** — it is NOT injected when a post-processor fires from a form save, so `getPropertyString("readerUrl")` returns `""`. ClickHouse JDBC then failed with `No suitable driver found for ` (empty URL — driver WAS present) and GoldMartClient silently fell back to CACHE even with ClickHouse up | Engines carry **code-level DEV defaults** and use `cfg(name, default)` (blank→default) for every connection property. Property JSON `value` kept for the console UI only |
| 2026-06-12 | CMBB-F09 | OSGi: clickhouse-jdbc multi-release JAR | `com.clickhouse:clickhouse-jdbc:0.6.0-patch5:all` is a multi-release JAR; bnd warns "Classes found in the wrong directory: META-INF/versions/9|11/…". Non-fatal — the embedded jar's base classes load and **live JDBC reads/writes to ClickHouse work** (T-09.3/05/06 LIVE) | Embed the shaded `all` classifier (compile scope, Embed-Dependency); `Class.forName("com.clickhouse.jdbc.ClickHouseDriver")`. Warnings accepted; revisit only if a versioned class is ever needed at runtime |
| 2026-06-12 | CMBB-F09 | test: dedup + Gold TIN charset | Reusing a TIN across two TEST cases tripped the TransitionGuard dedup (per type+TIN) → openCase TRANSITION_REJECTED, case never opened. And Gold seed TINs are `\d{6}[MGHABL]` only — `…K`/`…N`/`…Z` TINs used elsewhere are NOT in sta_v1, so LIVE balance reads come back empty | Acceptance fixtures use **distinct, Gold-present TINs** (100010A/100058G/100077B/…) for cases that must open+close+ship; verify with `SELECT tin FROM sta_v1.taxpayer_balances` |
| 2026-06-12 | SPIKE-001 | cross-app form over a shared table | A form **defined in app B** bound to `tableName` of a table app A owns (`dmbb`'s `dmSpineProbe` → `cmCase`) unions its column into the **global** `app_fd_*` table **additively on import** (27→28 cols) and **does not drop** the other app's columns; reads/writes/engines operate on the row regardless of which app's form definition touched it (CMBB `cmChainCheck` ran `VERIFIED` over a dmbb-written `cmCase` row) | This is the mechanical basis of ADR-001 B1 (module app rides the shared `cmCase` spine). `updateSchema` is append-only (never drops) — the same property behind version accumulation. Confirmed: `app_fd_*` is instance-global (SAD §6) in practice, not just in theory |
| 2026-06-12 | SPIKE-001 | `api_credential.apikey` is UNIQUE per row | Cloning `cmbb`'s `apikey` value onto an `API-dmbb-data` row fails the unique constraint (`INSERT 0 0`); a deployed app's API definition does NOT auto-create a credential | Each app's data API needs its **own distinct** key, bound once per app (`md5(random()…)` for DEV, or the console "generate key"). `load_md_seed`/runners pass `api_id=API-<app>-data` + that app's key. Cross-app metamodel seeding (DM `mmCaseType` into the CMBB-owned table) uses `API-cmbb-data`; module-local forms use `API-dmbb-data` |
| 2026-06-12 | DMBB-F03 | start the envelope from a plugin (cross-app) | A module engine (running in the `dmbb` form-save context) can create a `cmCase` row via FormDataDao and **start the cmbb envelope** for it. The process def must be resolved explicitly (the running app ≠ the process app): `appService.getPublishedAppDefinition("cmbb")` → `getWorkflowProcessForApp("cmbb", appDef.getVersion().toString(), "cmCaseEnvelope")` → `workflowManager.processStart(proc.getId(), null, vars{caseId,assignee}, null, recordId=caseId, false)`. Confirmed live (8/8 starts, 0 failed) | This is the reusable **CaseStarter** capability (DebtIdentificationJob / future auto-creation). caseRef + OPEN are still assigned by the existing TransitionGuard at openCase — the starter does not duplicate guard logic. Bind `var_assignee` so openCase lands in an inbox |
| 2026-06-13 | DMBB-F04 | cross-suite test isolation on shared spine | With all modules' cases in the one global `cmCase` table, a suite's GLOBAL count/dup assertion catches OTHER suites' rows: run_t12's `GROUP BY tin HAVING count>1` flagged run_t11's *intentional* rejected-duplicate (a 2nd `cmCase` row for the same TIN that openCase rejected), and run_t13 colliding with F03's Gold-identified TIN | Scope cross-entity assertions to the **suite's own data** (`AND tin IN (<this suite's set>)`); give each suite **TINs that don't overlap** the Gold-identified set (synthetic `T13<run>` for manual cases). Also: running many envelope suites in sequence leaves a stale-process backlog → occasional order-dependent transients (e.g. run_t05 T-05.2b); a suite passes in isolation |
| 2026-06-12 | DMBB-F03 | identification tests must be wipe-free | An auto-creation engine that **reads** `app_fd_cmcase` for dedup hits the F02 Hibernate-cache poison if the test SQL-DELETEs DM cases first: the engine reads the stale cached rows and skips everything (`created=0 skipped=N`). Cost a confusing red run | Identification/dedup acceptance asserts the **end state** (every Gold debtor has exactly one DM case) and **never SQL-deletes spine rows**; a Tomcat restart clears an already-poisoned cache. Generalises the F02 delta to read-then-dedup engines |
| 2026-06-13 | DMBB-UX-QA | datalist sortable/filter/drill are opt-in, not default | gen_datalists emitted plain columns WITHOUT `sortable`, `actions/rowActions` `[]`, and filters only where hand-specced — so lists had no sorting, no drill-down, weak filters (the FIS/TRACE had over-claimed "filterable/sortable/drill"). Verified shapes (joget-datalist-gen + live): column `"sortable":"true"`; `SelectBoxDataListFilterType` (enterprise) / `TextFieldDataListFilterType` (core); `org.joget.lst.DateRangeFilterType` is NOT installed on jdx9 | gen_datalists now sets sortable on every column, supports filter `type` select/text + auto TIN/status filters on form-companion lists, and `HyperlinkDataListAction` row/column drill; gen_userview gives DataListMenus a `customId` for clean drill URLs. Date-range deferred (plugin absent) |
| 2026-06-13 | DMBB-UX-QA2 | datalist column labels leak form-field provenance | gen_datalists used the verbose form-field labels as list headers, so columns read "Status (set by engine)", "Last escalation step seq (managed by EscalationEngine, F04)", "First assessed date … [DM-FR-045/BR-DM-038, F09]", "Strategy code (FK)" — 61 leaky headers | gen_datalists `clean_label()` strips `[..]` and ()-segments containing implementation keywords (set by/managed by/FK/engine/BR-DM/DM-FR/…) while keeping informative parentheticals ("(EUR)", "(C1-C6)", "(days)"). 61 → 0; form-field labels themselves are unchanged (still verbose in the form designer) |
| 2026-06-13 | DMBB-UX-QA2 | a JDBC list honours only ONE `#requestParam` filter at a time | Passing two filter params in one URL (`?ftin=X&finstr=LIEN`) substitutes only one — the other `#requestParam.Y#` resolves to '' so its guard is a no-op (verified: the row appears under the wrong instrument). Each filter works ALONE (`finstr=ZZNONE`→empty, `fcat=C5`→filtered) | Single-param drills (all the cell/row drills emit exactly one param) are reliable and tested. Multi-criteria filtering on a JDBC list is a recorded limitation — for true multi-filter, a form-row binder (native filters) or a combined `#requestParam#` expression is needed. Acceptance tests assert single-param discrimination, not param-combination |
| 2026-06-13 | DMBB-UX-QA | JDBC datalist filter + drill = `#requestParam.X#` in the SQL | A JdbcDataListBinder does not auto-apply the datalist `filters` to its SQL; the value reaches the SQL only as `#requestParam.<filterParamName>#`. So a filter dropdown and an inbound drill link both set the same request param, and the SQL must guard it optionally: `('#requestParam.fcat#' = '' OR col = '#requestParam.fcat#')`. Joget substitutes empty when absent (→ shows all); summary→detail drill is a HyperlinkDataListAction passing `?fcat=<cell value>` | Author the guard per filter; drill `hrefParam` = the same param. Caveat: a test harness that runs the DEPLOYED SQL raw in psql must `re.sub(r'#requestParam\.[^#]+#','')` first (else the literal guard filters everything out) — run_t20 fixed |
| 2026-06-13 | DMBB-RPT | JasperReportsMenu jrxml must be language="java" | A `JasperReportsMenu` compiles its inline jrxml at runtime; with `language="groovy"` Joget's bundled Groovy 2.4 throws `ClassCastException: String cannot be cast to [Ljava.lang.Object;` on the JDK21/Tomcat11 runtime (DX9). The JDT `java` compiler is bundled and JDK21-safe | Author every jrxml with `language="java"`; expressions must be valid Java with no checked exceptions (no `SimpleDateFormat.parse(...)` — format `new java.util.Date()` instead). All 6 DMBB reports render clean (run_t22) |
| 2026-06-13 | DMBB-RPT | JasperReportsMenu grid props are JSON arrays | The menu casts `parameters` to `Object[]`; setting `"parameters": ""` reproduces the same `String cannot be cast to [Ljava.lang.Object;` page. `export` is the exception — a `;`-joined string (`"pdf;xls"`). `datasource": ""` = the profile DB (PostgreSQL `jwdb_mtca`), which is where `app_fd_*` live | `gen_userview.py` jasper emitter always emits `parameters: []` (array) + `export: "pdf;xls"` (string) + `datasource: ""`; reports ship config-as-code through gen_userview→build_jwa→deploy_jwa, jrxml is the source of truth |
| 2026-06-13 | DMBB-F08 | seed CSV: quote any value containing a comma | `load_md_seed` uses the csv module, so an UNQUOTED field with a comma splits — F04's `mdTemplate` bodies ("Dear #taxpayerName#, this is a formal demand…") were silently truncated to "Dear #taxpayerName#", the remainder spilling into discarded columns. Invisible for two slices (F04 notifications only checked SENT status) until F08's template PREVIEW rendered the body | Wrap any seed value that can contain a comma in double quotes; F08 re-seeded the 5 F04 templates + 7 new ones quoted. When a stored value looks cut at a comma, suspect an unquoted seed, not the renderer |
| 2026-06-13 | DMBB-F08 | extending a deployed MD form is additive | Adding maxAmount/taxpayerType/instructions to the F01 `mdInstrument` spec and regenerating unions the new columns into the live `app_fd_mdinstrument` on import (SPIKE-001 append-only); existing seed rows keep working with the new columns blank. A consistency check should treat newly-added attributes as soft/recommended so the pre-existing catalogue still validates | Edit the original feature's form spec, regenerate, redeploy; VALIDATE counts only hard errors (dangling refs, missing fees), reports attribute gaps as warnings |
| 2026-06-13 | DMBB-F06 | seed needs its MD form, not just the CSV | A feature that introduces master data (mdRelief/mdViolation/mdProjRate) shipped only the seed CSV; `load_md_seed` 400'd on every row and `app_fd_mdrelief` did not exist — the API data path needs a deployed **form definition** to create the table and accept rows. Easy to miss because the engine reads the table fine via `updateSchema` (which creates it lazily for the *engine* but not for the API) | Every new MD carrier ships a `F-<md>.spec.yml` form spec in the feature `forms/` dir (generated → table created on deploy) BEFORE its seed CSV loads; the seed dir is for data only. Generalises: any `app_fd_X` an engine reads AND a seed writes must have a form |
| 2026-06-14 | DMBB-UX-QA3 | a form-companion datalist has no standalone `/_/<listId>` menu URL | A list shown only through a `CrudMenu` (the usual case for an `app_fd_*` form) is reachable in the UI at `/_/<menu customId>` (e.g. `/_/dmAction_crud`), NOT at `/_/list_dmAction` — the latter resolves to no menu and Joget returns an **empty 200 body** (no error, no rows). A render test that fetches `/_/<listId>` for a companion silently "passes" on the blank page | To verify a companion list renders, resolve its menu from the deployed `app_userview` json (`menus[].datalistId == listId → customId`) and fetch THAT. Only lists exposed by a `DataListMenu` (e.g. the JDBC detail lists) own a clean `/_/<listId>` URL via their `customId`. run_t25 does this; an intentionally-unexposed companion (list_dmDebt — the user-facing debt list is the joined list_dmCaseConsole) is whitelisted, not render-checked |
| 2026-06-14 | DMBB-UX-QA3 | curate wide companion lists with a `listColumns` form hint | `gen_datalists.py` emits one column per form field, so a wide subject form (dmAction = 18 fields) becomes an 18-column wall. Add `listColumns: [id, ...]` under `form:` in the form spec — an ordered subset (key field first) the companion list should show; the generator uses it verbatim (and skips the SKIP_TYPES filter so an explicitly-listed field always appears). Absent the hint, behaviour is unchanged (all non-skip fields) | Author `listColumns` on any form whose companion list is wider than ~8 columns; lead with tin/caseRef/code. 8 DMBB forms curated this way (18→7 worst case). The hint lives in the spec, not the generated json (P5) |
| 2026-06-14 | DMBB-UX-QA3 | clean_label strips ALL parentheticals except a units/codes allowlist | QA2's `clean_label()` only removed `()`-segments matching a fixed leak-keyword regex (set by/managed by/FK/…), which left FK-source refs ("(mdInstrument)", "(dmCharge)") and explanatory glosses ("(debt being enforced)", "(informational writeback)", "(SUCCESS/PARTIAL/FAILURE)") as grid column headers — noise that belongs on the data-entry form, not the list | Rewrote to KEEP a `()`-segment only if its content matches `^(EUR|USD|MDL|GBP|RON|%|C\d(-C\d)?|C1-C6|days|months)$`, else strip it. Subsumes the old leak set and removes glosses; keeps genuinely informative units/ranges ("(EUR)", "(C1-C6)", "(months)"). Verified zero gloss parentheticals across the 8 curated lists (run_t25 T-25.6) |
| 2026-06-14 | CMBB-F09 | ClickHouse JDBC: pin `http_connection_provider=HTTP_URL_CONNECTION` | The clickhouse-jdbc `-all` shaded jar's INSERT/failover path engages the shaded Apache HttpClient5 pool, whose `PoolingHttpClientConnectionManager.<clinit>` references `org.slf4j.LoggerFactory`. This OSGi bundle imports packages with `Import-Package: !*,<list>` + `DynamicImport-Package:*`, and the dynamic import does NOT resolve org.slf4j at class-init → `ExceptionInInitializerError: NoClassDefFoundError: org/slf4j/LoggerFactory`, which marks the JTA tx rollback-only (API HTTP 500). Worse, the failed `<clinit>` **poisons the shaded HTTP-client class for the whole JVM**, so once any ClickHouse call touches the Apache pool (e.g. the F09 degraded-read failover in run_t09 T-09.5), every later ClickHouse call in that JVM fails — t09 passed once on a fresh JVM but failed on every subsequent run, so a full sweep (t09 at position 9) always failed | Build every ClickHouse `Connection` with a `Properties` carrying `http_connection_provider=HTTP_URL_CONNECTION` (+ user/password) and `DriverManager.getConnection(url, props)`. The lightweight HttpURLConnection transport needs no slf4j, so the Apache/slf4j path is never touched and the engines stay repeatable. Pinned at BOTH gateways — `JdbcOutcomeGateway` (writer) and `JdbcGoldGateway` (reader/scan, 3 sites). Embedding slf4j-api or a static `Import-Package: org.slf4j` are alternatives but risk binding conflicts / bundle-resolution failure; the provider pin is the low-risk fix |
| 2026-06-14 | regression harness | a stateful suite must drain running envelopes before it resets | run_t03 (round-robin) and run_t08 (suppression→dispatch) raw-SQL `DELETE` their fixture tables at suite start, but the AllocationEngine round-robin cursor is a GLOBAL `count(cmEvent CASE_ASSIGNED)`. In a tight back-to-back sweep, a prior suite's still-running envelope can emit a CASE_ASSIGNED event AFTER the DELETE, skewing the cursor parity → t03 passed cold/isolated but failed in-sweep (likewise t08's dispatch). Fix: a `drain()` that polls `app_report_process_instance WHERE finishtime IS NULL` to 0 (stable for 2 reads, ~45s cap) BEFORE the reset; cold runs return instantly. Also made T-03.1 assert round-robin ALTERNATION ({a1,a2}=={officer1,officer2}) not absolute order, so the test never bakes in the cursor's starting parity | Any acceptance suite that (a) zeroes a table an engine reads as a GLOBAL count and (b) can run after another suite must `drain()` first; assert relative/alternating outcomes, not absolute ordering tied to a global counter. `scripts/run_regression.sh` is the canonical sweep — it also exports `MAYAN_USER`/`MAYAN_PASSWORD` from the live autoadmin pw (else run_t07's token obtain 400s on the admin/admin fallback) |
| 2026-06-14 | DMBB-DASH | userview custom-HTML dashboards: `HtmlMenu` does NOT exist; `HtmlPage` won't emit script | DX9 has no `org.joget.apps.userview.lib.HtmlMenu`. The custom-HTML menu is `org.joget.apps.userview.lib.HtmlPage` (content property = `content`), BUT it is a Quill rich-text page — the deployed `content` (with `<canvas>`/`<script>`) is stored verbatim yet does NOT render at runtime (no `<script>`/`<canvas>` reaches the page), so a Chart.js dashboard via HtmlPage shows nothing. For live in-app charts the proven paths are the enterprise `org.joget.plugin.enterprise.SqlChartMenu` (SQL→chart natively, one chart per menu) and/or a `FormMenu` over a form carrying a `CustomHTML` form element (renders raw HTML+JS). The same-origin "fetch the server-rendered datalist page and parse its table" data mechanism is sound (validated) — it just needs a host that actually executes the script (FormMenu/CustomHTML), not HtmlPage | Build dashboards as SqlChartMenu (multi-stream charts) + a CustomHTML-form FormMenu for KPI traffic-light tiles; do NOT use HtmlPage for scripted content. gen_userview has a `dashboard` emitter + gen_dashboards.py (3 tiers) ready to retarget at the working menu type |
| 2026-06-14 | DMBB-DASH | GroupPermission did not recognise seeded admin membership | Gating userview categories with `org.joget.apps.userview.lib.GroupPermission(groupId=<g>)` correctly hid each category from non-members — but the `admin` user, despite `dir_user_group` rows for dm_officer/dm_policy_admin/dm_manager + a Tomcat restart, was NOT treated as a member, so EVERY gated category became invisible to admin (home redirected to the first OPEN category; gated lists rendered empty). That breaks per-role landing for admin and the admin-based render acceptance tests (run_t23/24/25). Likely a DirectoryManager/organization-scope nuance (the existing dm_supervisor/cmbb_user groups have organizationid NULL; admin's primary org/group resolution may not see ad-hoc dir_user_group inserts without going through the directory API) | Until resolved, gen_userview emits OPEN permissions (gating code kept, commented). To finish: create the groups + memberships via the Joget directory API (not raw dir_* inserts), confirm `admin` resolves as a member (or use a role/Organization the userview honours), and give the acceptance tests role-user logins so gated categories can be verified per role. Role seed (dmbb/seed/dir_roles.sql) + the GroupPermission emitter are in place |
| 2026-06-14 | DMBB-DASH | dashboards = native SqlChartMenu (ADR-002), NOT custom HTML | The solid path for in-app charts on DX9 is the bundled enterprise `org.joget.plugin.enterprise.SqlChartMenu`: it runs data SERVER-SIDE and renders Apache ECharts with zero custom code. Bind it with `datasource:"datalist"` + `datalistId` (reuses an existing tested JDBC datalist binder — no SQL duplication) or `datasource:"default"` + raw `query` on the profile DB; pick columns with `keyName` (category/x) + `value` (numeric/y); `chartUseAllDataRows:"true"` charts every row (not just the current page); `chartType` bar/donut/line/pie/area/xy; `library:"echart"`. The chart loads its series via AJAX, so the category data is NOT in the initial page HTML (don't assert inline data — assert the bound datalist returns rows; the binder SQL is tested separately). `datasource:"custom"` (driverClassName/url/username/password) makes it re-pointable to ClickHouse/ITCAS at go-live. Composing several into one page = `org.joget.plugin.enterprise.DashboardMenu` (portlet grid, `mode:"plugins"`). `gen_userview.py` gained a `sqlchart` emitter; ships config-as-code through the normal gen→build→deploy loop like Jasper | Build dashboards from SqlChartMenu over the MI datalists; never scrape rendered datalist HTML and never hand-roll Chart.js in a CustomHTML/FormMenu (the rejected interim — ADR-002). run_t26 verifies all 4 charts render server-side + the interim is retired |

## 2026-06-17 — DMBB-FORM-QA: read-only/managed fields + LOV + state-machine design
| Date | Batch | Artefact class | Finding | Resolution |
|---|---|---|---|---|
| 2026-06-17 | DMBB-FORM-QA | gen_forms / forms | Managed/engine-owned fields (status, currentState, caseRef, assignee, debtCategory, complianceStatus, writeOffStatus, all run-form "set by engine" results) were authored as plain `textfield` and rendered as editable inputs — a user could overwrite engine state (e.g. type "CANCELLE" over CANCELLED). | `gen_forms.py` gained `managed`/`readonly` (field- AND section-level) + a label-aware auto-lock (honours "(set by engine)/(managed by …)/(copied from …)" annotations) → `readonly:true`. FK fields (instrument→mdInstrument, ground→mdWoGround, method→mdEstMethod) converted to `select`+`lookup` (FormOptionsBinder) with `extraCondition` (instruments filtered to enabled). |
| 2026-06-17 | DMBB-FORM-QA | **PLATFORM** | A `readonly:true` form field is **NOT persisted via the REST data API** (`/api/form/{id}/saveOrUpdate` runs the form pipeline, which drops submitted values for read-only fields). Engines writing via `FormDataDao` are UNAFFECTED (lower level, bypasses readonly). Surfaced as 5 acceptance suites (t11/16/18/20/21) failing because their fixtures seeded the managed `category` routing key via the data API → stored empty → downstream category gates (publish/write-off/debtors-list/MI) failed. | Production is correct (identification engine sets category via FormDataDao). Fixed the **test fixtures**: after the API create, seed the managed fields (category/stage/consolidatedAmount/currentState/…) via a raw-SQL `UPDATE` (the documented fixture exception); cross-request so the engines reload fresh. Rule for future specs: a field set only by an engine = `managed:true`; never seed a managed field via the data API — use FormDataDao or a SQL fixture. |
| 2026-06-17 | DMBB-FORM-QA | userview / data | Admin menu labels leaked DPM domain codes "(D1)/(D2)/(D3)"; mdInstrument exposed under two menus (F01 "Enforcement instruments" + F08 "Action types"); mdInstrument polluted with 19 ZZTEST* rows from test runs (37→18). | Stripped the "(Dn)" suffixes; removed the duplicate F08 instrument menu; purged ZZTEST* rows. (Open: teach the test suites to clean up their own tagged fixtures.) |
| 2026-06-17 | DMBB-LINEITEM | forms / plugin / Gold | Debt-line legal-correctness: each dmLine now itemises principal / interest / penalty (+ tax type as a dropdown, period); tax type removed from the case header (a case is consolidated across tax types). The STA Gold `debt_balances` view already exposed the PA/IA/PCA decomposition — `JdbcGoldGateway.lines()` + `GoldLine` + `DebtIdentificationService` thread principal=pa/interest=ia/penalty=pca onto each line; `F-dmLine` adds the columns + the dmCaseConsole grid shows them. Verified: 77/94 lines populated, principal+interest+penalty == total on every Gold-identified line; **full cold-start regression GREEN=25**. |
| 2026-06-17 | CMBB-F10-SM | forms / plugin / seed | Status state-machine FOUNDATION (ADR-003, dormant). Native, no DB/no external bundle: two CMBB metamodel forms `mmEntityState`/`mmEntityTransition` (entity+scope) + `MmConfigService` entity lookups + native `StatusManager` (reuses existing `InvalidTransitionException`; writes status via FormDataDao, audits via `cmEvent` STATUS_CHANGED; scope resolution taxType▸caseType▸DEFAULT wholesale shadow). Seeded baseline DM lifecycles for 6 entities + a VAT `dmDebt` override (67 rows). NOT wired to engines yet. **run_t27 6/6 + full cold-start regression t02..t27 GREEN=26.** Engines migrate entity-by-entity in later passes. |
| 2026-06-17 | CMBB-F10-SM | plugin / test / seed | **Engine migration #1 — `dmInstLine` (ADR-003 §5 step 2, lowest-risk first).** `ReliefService.evaluateLines` no longer does `l.setProperty("status",…)`; it calls `StatusManager.transition("dmInstLine","dmInstLine","status", lineId, debtCaseId, target, ["DM"], actor, reason)` so a due line's `pending→paid/missed` is config-guarded and audited as `STATUS_CHANGED` on the case hash-chain (same `CaseEventWriter` instance → one chain). No-op (status unchanged) is skipped; added seed `missed→paid` (late settlement — the engine recomputes outcome every compliance run, so that move must be legal). Initial `pending` write in `apply()` stays a direct seed (no from-state). **Two test gotchas:** (1) `ReliefServiceTest`'s Mockito fake had to stub `dao.count` (used by `entityScopeHasRules` for scope resolution) and seed the dmInstLine lifecycle into its store, else every move is "illegal [scope=DEFAULT]"; (2) the fake's `find` stub used `anyInt()` for the start/limit args, but `MmConfigService.entity*` calls pass `null,null` — `anyInt()` does NOT match null, so the stub returned no rows → broaden those two matchers to `any()`. Result: 129 JUnit green (incl. `StatusManagerTest` illegal-rejection), run_t15 **T-15.5** live audit (2 missed STATUS_CHANGED + 1 pending), full cold-start regression **t02..t27 GREEN=26**. |
| 2026-06-17 | CMBB-F10-SM | plugin / test / seed | **Engine migration #2 — `dmAction` (ADR-003 §5 step 2).** `EnforcementActionService` (initiate/execute/reject + confirm sweep) routes all 10 status writes through a NEW `StatusManager.apply(entity, FormRow row, …)` — a **row-based** guard: validates current→target and sets the field on the caller's already-loaded row, leaving persistence to the engine's existing single `saveOrUpdate` (so batched field writes aren't lost), and audits `STATUS_CHANGED`. (`transition()` remains for the load-write-audit-in-one case, e.g. dmInstLine.) **Read-only-field corollary (extends the 2026-06-17 delta):** because `dmAction.status` is read-only the data API drops the creator's `INITIATED`, so the row arrives blank at runtime — a guarded `""→EXECUTED` is (correctly) illegal where the old unguarded `setProperty` silently overwrote. The engine must therefore ESTABLISH the canonical initial: `initiate()` sets `status=INITIATED` in-memory when blank before the first guarded move. Rule for future engine migrations: any entity whose initial status is supplied only via the (read-only) data API needs the engine to set the initial before transitioning. Seed gained `INITIATED→FAILED` (garnish decline; the pre-existing `SUBMITTED→FAILED` was unused). Unit test fake DAO: broaden the `find` matcher `anyInt→any` (null start/limit) + seed the dmAction lifecycle. Live: run_t16 **T-16.9** (EXECUTED + BLOCKED → STATUS_CHANGED). Full cold-start regression **t02..t27 GREEN=26** (run_t16 9/9, run_t27 6/6). |
| 2026-06-18 | CMBB-F10-SM | plugin / test / seed | **Engine migration #3 — `dmWriteOff` (ADR-003 §5 step 2).** `WriteOffService` (submit/approve/post/sweep) routes every status write through `StatusManager.apply`. The shared `post()` (the `→POSTED` terminal write) is reached from three current states — `SUBMITTED` (auto-C1), `UNDER_REVIEW` (approve), and a freshly-created sweep row — so the canonical initial `SUBMITTED` is established at BOTH creation points (submit() normalises the read-only-dropped value; the sweep's `autoWriteOff` sets it on the new `FormRow` before `post()`). **Seed reconciled to engine reality:** the original two-step `UNDER_REVIEW→APPROVED→POSTED` is NOT what the engine does — approve posts in one move, so added `UNDER_REVIEW→POSTED`; also added `SUBMITTED→REJECTED` (evidence-missing rejection happens straight from submit). General lesson: author the lifecycle from the engine's actual writes, not the idealised paper workflow — a missing edge only surfaces at runtime as an `InvalidTransitionException`. Unit-test fake DAO got the now-standard `find` matcher `anyInt→any` fix + lifecycle seed. Live: run_t18 **T-18.8** (approve chain 2× + reject 1× STATUS_CHANGED). Full cold-start regression **t02..t27 GREEN=26** (run_t18 8/8, run_t27 6/6). |
| 2026-06-18 | CMBB-F10-SM | plugin / test / seed | **Engine migration #4 — `dmDefAssess` (ADR-003 §5 step 2).** `DefaultAssessmentService` (assess/replace/escalate) routes status writes through `StatusManager.apply`. First **pre-debt** entity migrated: a default assessment has no debt case until it escalates, so its cmEvent audit anchors under a synthetic `DA-<assessId>` key and SWITCHES to the new debt case on escalate (set `debtCaseRef` BEFORE the guarded move so the audit + anchor land on the new case's chain). `assess()` normalises the read-only-dropped `DRAFT`; the `NEEDS_JUSTIFICATION` write is no-op-guarded for re-assessment. **Guard-as-feature:** `replace()` has no status precondition, so `DRAFT/NEEDS_JUSTIFICATION→REPLACED` are seeded (a return filed before/instead of assessment) but `ESCALATED→REPLACED` is intentionally omitted — the state machine now REJECTS replacing an already-escalated assessment (a debt case already exists), a correctness gain the old unguarded `setProperty` lacked. Unit-test fake: `find` matcher `anyInt→any` + lifecycle seed. Live: run_t19 **T-19.6**. Full cold-start regression **t02..t27 GREEN=26** (run_t19 6/6, run_t27 6/6). |
| 2026-06-18 | CMBB-F10-SM | plugin / test / harness | **Engine migration #5 — `dmInstAgr` (ADR-003 §5 step 2).** `ReliefService` apply/reject/cancel now guard the agreement status: `APPLIED→ACTIVE/UNDER_REVIEW/REJECTED`, `ACTIVE→CANCELLED`; `apply()` normalises the read-only-dropped `APPLIED`. The engine never sets `COMPLETED`, so the seeded `ACTIVE→COMPLETED` stays unused (harmless). Seed needed NO change (the dmInstAgr edges were authored in the foundation). Live: run_t15 **T-15.6**. **Repo-move incident:** mid-migration the working tree was relocated `15-ITCAS/03_debt_management → 10-workstreams/itcas-programme/03_debt_management`; the native file tools couldn't reach the new path (used desktop-commander instead), and `scripts/run_regression.sh` hard-coded the old `ROOT` and died at `cd`. Fixed the harness to derive `ROOT="$(cd "$(dirname "$0")/.." && pwd)"` so it survives future moves. Full cold-start regression **t02..t27 GREEN=26** (run_t15 6/6, run_t27 6/6). |
| 2026-06-19 | CMBB-F10-SM | plugin / test / seed | **Engine migration #6 — `dmDebt.stage` (ADR-003 §5 step 2, CAPSTONE — all six entities now migrated).** `EscalationService.walk` guards the subject stage ladder. **Two hard lessons:** (1) **Author the lifecycle from the engine's REAL writes** — the foundation seed used idealised stage names (`Demand`, `Enforcement`) but the strategy `stepName`s are `Demand notice` / `Bank garnishing` / `Field enforcement`; reconciled the dmDebt mmEntityState+mmEntityTransition (DM+VAT) to the real names. The idealised seed had been dormant, so live data already used the real names — no data migration needed. (2) **A global batch sweep must be resilient to one bad record.** The guard immediately exposed a latent corruption: leftover SQL-fixture cases (`stage='Final demand'`, `lastStepSeq=0`, from run_t16) caused the walk to re-fire step 1 → the old unguarded write silently REGRESSED the stage (`Final demand → Reminder`); the guard threw `InvalidTransitionException`, which aborted the ENTIRE sweep so nothing escalated. Fix: pre-check `status.canTransition(...)` in the walk and `break` (skip) an inconsistent case instead of regressing it — a silent-corruption bug becomes a safe no-op AND the batch no longer aborts on one malformed record. **Scope is `["DM"]` not per-tax:** the engine picks its strategy by segment+category (tax-agnostic), so every tax runs the same ladder; the seeded VAT override (skip Reminder) remains a config-proven capability (run_t27 T-27.4 / StatusManagerTest) to be wired once strategy selection is tax-aware. Live: run_t13 **T-13.5**. Full cold-start regression **t02..t27 GREEN=26** (run_t13 5/5, run_t27 6/6). |
| 2026-06-19 | CMBB-F10-SM | plugin / test | **Per-tax escalation pilot (ADR-003 §5 step 3, engine half).** Made `EscalationService.walk` per-tax WITHOUT a new strategy or schema change: scope = `[caseTaxType, "DM"]`, and when a strategy step's stage isn't legal in the case's scope lifecycle (e.g. a VAT case at `Identified` with the seeded VAT override that has no `Identified→Reminder`), the step's **time-slot is consumed** (counter advances, no notice, stage NOT regressed) and the walk continues — so the next lifecycle-legal step applies in its own slot. One strategy template; per-tax behaviour comes entirely from the lifecycle config. This also self-heals a stage/counter mismatch (resume at the real stage instead of the migration-#6 plain `break`). Lesson: `cmEvent` has NO `c_fromstatus/c_tostatus` columns — from/to live inside the hash-chained `c_payload`; assert stage outcomes + notice templates, not from/to columns. run_t13's main case was tagged VAT but asserts the Reminder ladder — retagged it `INCOME_TAX` (no override → default DM ladder) and added **T-13.6** (a VAT case: 0 Reminder notices, straight to Demand notice, 1 STATUS_CHANGED). `EscalationServiceTest` got a scope-aware fake + `vatSkipsReminderViaLifecycle`. run_t15's VAT case is unchanged (consumed Reminder still advances the counter, so seq stays 3). Full cold-start regression **t02..t27 GREEN=26** (run_t13 6/6, run_t27 6/6). |
| 2026-06-19 | DMBB-USERVIEW | userview / directory | **Per-role landing + GroupPermission gating (task #60) — mechanism built, gating OPT-IN/OFF; blocked on a Joget directory quirk.** `gen_userview.py` now maps the 4 buckets to role groups (Dashboards→dm_manager, Operations→dm_officer, Automation/Admin→dm_policy_admin) and emits a `GroupPermission` per category; per-role *landing* then comes for free (Joget lands a user on the first category they may see). Regeneration is faithful — diffed against the deployed userview, the per-bucket menu SETS are byte-identical and ONLY the permission block changes. **Live probe verdict (deployed gated, restarted, rendered):** anonymous is correctly denied ALL categories (gating WORKS), but an AUTHENTICATED `admin` — a valid, active member of all three groups via `dir_user_group` — still saw NONE of them. So Joget does not resolve `admin`'s flat-group membership at userview render time. Ruled out org-scoping: `admin` has no `dir_employment`, all groups are `organizationid IS NULL`, the lone org `ORG-001` has no members — yet membership still isn't recognised, so it's a `DirectoryManager` behaviour, not a data gap. **Action:** gating is gated behind `UV_GATING=1` (default OFF → OPEN, the safe shipped state); flip it on only after the membership resolution is fixed (create groups+members via the Joget directory API rather than raw `dir_*` inserts, or align the configured DirectoryManager). Reverted the live console to OPEN (regenerated, redeployed, restarted) — anonymous again sees all 4 buckets; full cold-start regression re-run to confirm the redeploy left the baseline green. The full-app `gen_userview → build_jwa → deploy_jwa` loop was exercised and works; reusing the existing `dmbb/generated/` forms+datalists (only the userview regenerated) keeps blast radius to the userview alone. |
| 2026-06-19 | DMBB-UX-QA | forms / datalists | **Two live UI defects fixed (user-reported).** (1) **dmAction edit rendered "null":** the instrument SelectBox's FormOptionsBinder `extraCondition` was authored with a leading `WHERE` (`"WHERE c_enabled='true'"`); Joget's binder PREPENDS its own `WHERE`, producing `WHERE WHERE e.customProperties.enabled='true'` → HQL `SyntaxException` → the whole edit form body renders literally as `null`. Fix: extraCondition must be the bare condition (`c_enabled='true'`) — Joget translates `c_X`→`e.customProperties.X` and adds the `WHERE`. Hardened `gen_forms.py` to strip a leading `WHERE`/`where` from any lookup extraCondition so this can't recur. (2) **Instalment-compliance drill `?fstat=<status>` showed "Nothing found":** `list_instalments` SQL selected `g.c_approvallevel`, a column that does NOT exist on `app_fd_dminstagr` (dmInstAgr stores `c_approvalref`, not approvalLevel — only dmWriteOff has approvalLevel) → the JdbcDataListBinder COUNT query threw `column does not exist` → the list rendered a SILENT empty (HTTP 200, no 500), so the existing "no-500" render check missed it. Fix: select `g.c_approvalref` (alias kept `approval_level`, column relabelled "Approval"). Hardened run_t24: **T-24.2** now requires `datarows>=1` (a binder column-error → empty list is now caught), and new **T-24.6** asserts the dmAction edit form renders a real `<form>` (not `null`). Regenerated dmAction + list_instalments only (reused the rest of dmbb/generated), redeployed, restarted, verified both pages live. Full cold-start regression **t02..t27 GREEN=26** (run_t24 6/6). |
| 2026-06-25 | DMBB-WF (ADR-004) | forms / seed | **Phase 1a — `dmWorkflow` carrier + step re-parent (additive, engine unchanged).** New config form `dmWorkflow` (DMBB-WF-workflow-config) carries the workflow selector (tenant × taxType × segment × industry, blank = ANY), a validity window (`validFrom` req / `validTo` optional), `categoryFloor`, `status` (Draft/Active/Superseded) and `version`. Added an additive `workflowCode` field to `mmEscStep` (FK to dmWorkflow). Seeded two workflows — `W-DEFAULT` (tenant MLT, tax ANY, floor C2, Active, the migrated STD-MLT ladder) and `W-VAT` (tax VAT) — and re-parented all five STD-MLT steps to `workflowCode=W-DEFAULT`. The escalation engine still read `strategyCode` at this point, so behaviour was unchanged. Deployed (49 forms), seeded, full cold-start regression **t02..t27 GREEN=26**. |
| 2026-06-25 | DMBB-WF (ADR-004) | plugin / test / seed | **Phase 1b — workflow resolver + overlap VALIDATE + escalation rewire.** New `WorkflowService`: `resolve(tenant, taxType, segment, industry, category, asOf)` selects the governing `dmWorkflow` over the dimension space — selector match (blank=ANY), validity window contains `asOf`, `categoryFloor` admits the band, and the **most specific** wins (specificity weighted tax 4 ▸ segment 2 ▸ industry 1, highest `version` breaks ties); blank case-tenant resolves via `DEFAULT_TENANT=MLT` (forward-compatible with Phase 1c). `findOverlaps()` reports any two Active workflows that share the exact selector tuple AND have overlapping validity windows (resolution-ambiguity guard) — folded into the existing `cmStrategyCheck` VALIDATE trigger via `StrategyAdminService.validate()` (run_t14 stays VALID; W-DEFAULT/W-VAT are distinct tuples). `EscalationService` now resolves a **workflow** (not a strategy); `walk()` reads steps by `workflowCode` with a **transitional fallback to W-DEFAULT** when a resolved workflow has no ladder of its own — so VAT (resolves W-VAT, which has no bespoke steps yet) inherits the default ladder and still skips Reminder via the per-tax lifecycle scope. Removed the now-dead `resolveStrategy`/`F_STRATEGY`/`rank`/`BANDS` from EscalationService. Tests: new `WorkflowServiceTest` (10 — specificity, validity exclusion, floor, tenant-default, version tiebreak, overlap detection/disjoint), updated `EscalationServiceTest` fake DAO to seed `dmWorkflow`. JUnit: 20 green across the three suites. Live: run_t13 6/6 (incl. T-13.6 VAT-skips-Reminder, now driven through the resolver), run_t14 3/3 (VALIDATE includes the workflow-overlap check). Full cold-start regression **t02..t27 GREEN=26**. |
| 2026-06-25 | DMBB-WF (ADR-004) | plugin / forms / test | **Phase 1c — invisible, profile-scoped multi-tenancy.** Tenant is now a pervasive but INVISIBLE scope (never a screen, never a picked field). New `TenantContext.resolve(username)` reads the admin-only `mdUserTenant` mapping (username→tenantCode, active) and falls back to `DEFAULT_TENANT=MLT` for unmapped users — so a single-tenant deployment needs zero per-user config. `OpenPhase` stamps the resolved tenant onto a case at fresh-open (managed `cmCase.tenant`, never shown/picked). The escalation resolver already keys on the case tenant (Phase 1b), so a case is governed only by workflows of its tenant; `EscalationService.walk` now also records the resolved `dmDebt.workflowCode` (observable proof + 1d foundation). New managed fields: `cmCase.tenant`, `dmDebt.workflowCode`, `dmDebt.tenant`; new form `mdUserTenant`; existing rows backfilled to MLT. Tests: `TenantContextTest` (4 — mapped/unmapped/inactive/blank), new live `run_t28` (T-28.1 a process-opened case is stamped tenant=MLT invisibly; T-28.2 tenant ISOLATION — an MLT case resolves W-DEFAULT, a TST case resolves a TST-only workflow). 144 JUnit green. **Regression caught + fixed (auto-lock retro-lock):** regenerating `cmCase.json` with the current `gen_forms` label-aware auto-lock newly marked `caseRef` ("set by guard"), `assignmentStatus` ("managed by …") and `slaStatus` ("managed by …") `readonly:true` — and since the REST data API silently drops readonly fields, the previously-editable `caseRef` stopped persisting, so reassignment (matched by caseRef) found 0 cases → t06 (0 NOTIF_PENDING) and t17.3 (blank merge fields) failed. Root-caused via the server log ("REASSIGN order: 0 case(s) matched") + the empty `caseRef` column. Fix: added explicit `readonly: false` on those three cmCase fields (the documented auto-lock override) to preserve their pre-1c editable state; `tenant` stays locked (engine-only, set via FormDataDao/SQL). Lesson: regenerating a long-deployed form re-applies the newer auto-lock to annotated labels — diff the lock flags after any regen of a form whose fixtures write via the data API. Full cold-start regression **t02..t28 GREEN=27, FAILS=none.** |
| 2026-06-26 | DMBB-WF (ADR-004) | plugin / test | **Phase 1d — workflow-driven case constitution at identification (ADR-004 §14).** `DebtIdentificationService` no longer makes one consolidated case per TIN. For each TIN it now resolves the governing workflow per tax line and CONSTITUTES the case(s) by tax-usage: a tax governed by a tax-specific workflow (selector pins taxType, e.g. VAT→W-VAT) forms its OWN single-tax case; every tax governed by a tax-agnostic workflow (or none) consolidates into ONE multi-tax case. Each case is stamped with its `taxType` (blank = consolidated), its summed enforceable `amountAtStake`/`consolidatedAmount`, and (on dmDebt) the governing `workflowCode`; escalation then resolves the same workflow from the case's taxType. Dedup unchanged (skip a TIN with any non-terminal DM case). `DebtIdentificationServiceTest`: fake DAO seeded with the dmWorkflow catalogue; `splitsTaxSpecificFromConsolidated` proves a VAT+CIT TIN → 2 cases (VAT-pinned + consolidated) with the workflow recorded; dedup expectations updated (a TIN now yields ≥1 case). **Live t12 rewritten to the constitution model** (was "1 case/TIN"): computes each in-scope Gold TIN's expected case count from its tax set (VAT is the only tax-specific tax in the seed), asserts per-TIN counts (8 TINs → 11 cases), and a split-detail check on TIN 100091L — its VAT case carries the 6 VAT lines / W-VAT, its consolidated case the 13 non-VAT lines / W-DEFAULT, category C6. Because a behaviour change leaves the pre-1d cases in the old shape and dedup then blocks re-constitution, the stale in-scope SYSTEM cases were cleared ONCE (SQL delete + restart to flush the Hibernate cache the dedup reads — DX9-DELTAS F02) so identification re-constitutes them; t12's assertions are END-STATE (robust to create-this-run vs persisted). 144 JUnit green. Full cold-start regression **t02..t28 GREEN=27, FAILS=none** (downstream debtors-list/MI suites unaffected). This completes ADR-004 Phase 1 (1a carrier → 1b resolver/VALIDATE → 1c multi-tenancy → 1d constitution); Phases 2–3 (tabbed workspace UI, operational/legislative nav split) remain. |
| 2026-06-26 | DMBB-WF (ADR-004 §6/§7) | forms / userview / test | **Phase 2 + 3 — configuration workspace + navigation tiering.** §6 workspace: the `dmWorkflow` form is now a 6-section workspace — **Definition** (selector + validity + status fields), an in-place **Escalation ladder** as an editable `FormGrid` over `mmEscStep` keyed by `workflowCode` (parent `dmWorkflow.id == code`, so the grid loads/stores the workflow's own steps), and **Lifecycle & stages / Notices / Enforcement / Relief & instalments** as concern panels (a new `html`/CustomHTML element in gen_forms; presentation-only, so no second grid writes the shared child table — zero data-loss risk). Entry is a tenant-scoped **Workflows** CRUD list (`list_dmWorkflow`, auto-generated) leading the operational category. Joget DX has no native clickable-tab element, so the workspace is delivered as labelled sections (the Dx theme provides section navigation) with the ladder fully editable in place; the FormGrid's rows load via AJAX. §7 nav tiering: `gen_userview` gained two buckets — **Collection settings** (operational) and **Legal & reference** (legislative) — replacing the broad **Admin**; menus are routed per the §7 `LEGISLATIVE_FORMS` set (mdInstrument, mdWoGround, mdWoDelegation, mdWoPolicy, mdLegalFee, mdAgentFee, mdEstMethod, mdDefAssessPolicy, mdPublishRule, mdViolation → Legal & reference; everything else operational → Collection settings, fronted by Workflows). Role gating stays parked (#60/#91); the taxonomy + labelling ship now. New **run_t29** (5/5): nav split present + Admin gone + Workflows leads; §7 split correct; workspace form carries the 6 sections + the mmEscStep ladder grid; Workflows list renders the seeds; the workspace **edit form renders all 6 sections + the ladder grid** (HTTP 200). **run_t25** updated from the 4-bucket to the 5-bucket console. Deployed dmbb (50 forms, +`list_dmWorkflow`, userview republished). Full cold-start regression **t02..t29 GREEN=28, FAILS=none.** Future enhancement: swap the concern panels for data-rich read-only lenses (EmbeddedDatalist) and add literal top-tabs once a tab plugin/section-nav theme is in. |
| 2026-07-01 | DMBB-RBAC (#91) | userview / directory / tests | **Per-role userview gating now WORKS — the "admin membership not resolved" symptom (2026-06-14/19 rows above) was a self-inflicted property-name bug, NOT a DirectoryManager quirk.** Root cause (bytecode-verified against `wflow-core-9.0.7` + `properties/userview/groupPermission.json`, see `docs/design/SPIKE-groupperm.md`): DX9 `org.joget.apps.userview.lib.GroupPermission.isAuthorize()` reads a `;`-delimited multiselect property **`allowedGroupIds`** and matches it against `directoryManager.getGroupByUsername(user).getId()`. `gen_userview.category_permission()` was emitting `groupId`/`isExclusive` (names the plugin never reads), so the allow-list was always empty → the match loop never ran → deny-all incl. admin (the whole console blanked with gating ON). The directory resolves admin's flat `dir_user_group` membership fine — it's the very same `getGroupByUsername` DAS P3's `AuthorityResolver` uses to resolve admin→DIRECTOR. **Fix:** emit `allowedGroupIds`; `UV_GATING` now defaults ON (`UV_GATING=0` disables); completed the directory seed (`dm_collection_admin`/`dm_legal_admin` were referenced by `BUCKET_ROLE` but never created — added them + admin membership; invariant: admin ∈ all role groups so superuser + admin-based render tests still see every category). Deployed via a **surgical per-category permission-block edit** of the on-disk userview (avoids the UV-delta ORDER trap). **Consequence for the harness (now that categories are truly gated): anonymous GETs of a menu page 302 to login**, so the 7 render tests that fetched userview pages anonymously (t22–t26, t29, t39) now fetch through an admin session via the new shared `scripts/uv_auth.py::admin_get` (their form-data API POSTs keep their own api-key auth — engine/roleAnonymous behaviour unchanged). New **run_t40** (DMBB-RBAC) proves the live signature: config carries the right non-empty `allowedGroupIds`; anonymous denied every category (302); admin (∈ all groups) served every category; per-role landing (officer1 ∈ dm_officer → only Operations) derived from the deployed config + directory. **Closes #60/#90/#91.** Rule: a userview permission property name must be taken from the plugin's own `properties/*.json`/bytecode, never guessed. |
| 2026-06-29 | DMBB-PREFILL | plugin / forms / userview / test | **Configurable, project-neutral form-prefill binder — replaces the hardcoded `CaseContextLoadBinder`; plus a latent userview-order trap surfaced by the sweep.** New standalone OSGi bundle `joget-form-prefill` (`com.fiscaladmin.joget.formprefill.FormPrefillLoadBinder`, own Maven module under `shared-plugins/`, depends only on `wflow-core` *provided* — zero cmbb coupling, so the same JAR drops into any Joget instance). On add-mode load it resolves a key (ordered sources: requestParam/loginUser/currentField), looks the source record up **via FormDataDao** (`find` primary + optional `load` related — NO raw SQL, per P3), applies eq/ne filters + numeric/lexical order + pick-first, and maps `key`/primaryField/`alias.field`/constants onto the form; edit/view defers to `super.load`; any failure falls back to the default load (never a 500). Driven entirely by plugin properties, so `gen_forms.py` gained `loadBinder: prefill` + a `prefill:` block (grids serialise as JSON arrays Joget hands back to the binder as `Object[]`-of-`Map` via `getProperty`). dmInstAgr + dmPayment re-pointed (config only; the `#requestParam.*#` field defaults removed; `CaseContextLoadBinder` retired out of the compile path + de-registered from the cmbb Activator; cmbb 144 JUnit still green). 12 new pure-resolver unit tests (fake DataAccess: key fallback, no-key, no-match, eq/ne filters, numeric pick-largest, alias mapping, constants, bad-field rejection, grid parsing). Live on jdx9: instalment shows the debtor's open DM case + outstanding (from the related `dmDebt.consolidatedAmount`); a two-open-case TIN picks the larger by `amountAtStake`; payment prefills tin+caseId; no-key/no-match prefill nothing. Cross-app reuse proven with a throwaway `prefilldemo` app (a different config — no related load — on the same already-deployed JAR). **Trap (PRE-EXISTING, not from this change):** `gen_userview` orders within-bucket menus purely by **UV-delta argv order**; the on-disk dmbb userview had drifted with `DMBB-WF-workflow-config` ordered late, so `Workflows` no longer led `Collection settings` → **t29 failed** the first sweep — latent because full regression had been deferred during the worklist/payments work, and `build_jwa` from disk faithfully redeploys whatever userview is on disk. Fix: regenerate the userview with `DMBB-WF-workflow-config` right after `DMBB-DASH` (only that one menu moves). Lesson: after ANY dmbb redeploy, regenerate the userview with the correct argv order if menu lead-position matters, and run the full sweep. Full cold-start regression **t02..t30 GREEN=28, FAILS=none.** |
| 2026-07-01 | DMBB-F14 (DM-FR-018/019/020, WF-FR-009) | engine / forms / userview / test | **Contact/visit tracking + high-value fast-track — and three reusable platform findings.** (1) **`FormDataDao.saveOrUpdate` silently DROPS properties that are not defined form fields.** The fast-track engine set `priorityBand`/`fastTrack` on `cmCase`, but those are not cmCase form fields, so the write persisted nothing (no column created) even though `priority` (a real field) saved fine. Rule: an engine may only write properties that exist as fields on the target form; to add a new attribute, add the field to the form (spine change) OR reuse an existing field. Here the CRITICAL marker is the existing numeric `priority` (=3, top band) + a `TELEPHONE_CONTACT` `cmDeadline` (the SLA clock the sweep monitors) + a `CASE_PRIORITISED` `cmEvent` (audit) — no spine form change. (2) **`dao.find(form, form, "WHERE e.customProperties.<flag> = ?1", …)` silently returns EMPTY for a brand-new form's flag**, even though the identical condition works for long-established forms (mdOfficerProfile.active). Use the proven **null-condition load-all + filter in Java** pattern (`dao.find(form, form, null, null, sort, desc, 0, N)` then check `getProperty("active")`), as CollectionMiService/DefaultAssessmentService do. (3) **`gen_userview` hardening:** `Approvals MI` (DAS P6 SqlChartMenu) is now a first-class bucket emitted by the loop (added to `BUCKET_ORDER`/`BUCKET_ICON`/`BUCKET`, gated `dm_manager`) — retires the fragile post-build "surgical insert" AND stops its 4 charts leaking into Operations when the console is regenerated from the globbed UV-deltas; and the within-bucket **argv-order trap** (2026-06-29 row) is now fixed ROBUSTLY — `main()` stable-sorts categories so `Workflow configuration` always leads `Collection settings` regardless of UV-delta file order (t29's "Workflows leads"). Also: the mock **`sta_v1` Gold tables are VIEWS** (not INSERT-able), so DM-FR-018's live acceptance identifies a REAL un-caased high-value debtor (101632G, ~127k, no VAT line → one consolidated case) with the threshold lowered, asserting priority=3 + CASE_PRIORITISED + TELEPHONE_CONTACT as an order-independent end-state. New **run_t41** (6/6): Call/Visit worklist quick-actions on the debtors list; dmContact + dmVisit saved, case-linked and listed; mdContactPolicy threshold deployed; high-value debt → CRITICAL + telephone SLA deadline. `DebtIdentificationServiceTest` +2 unit tests (fast-track fires above threshold; not below). Deployed dmbb (59 forms, 64 datalists, +dmContact/dmVisit/mdContactPolicy + list_contacts/list_visits). Full cold-start regression **t02..t41 GREEN=40, FAILS=none.** |
