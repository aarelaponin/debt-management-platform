# Session Restart Note — MTCA Debt Management Build
**Updated 14 June 2026 — 🎉 DMBB MODULE COMPLETE (CMBB F01–F09 + DMBB F01–F12, all Accepted) + RPT Jasper tier + 3 list-UX QA rounds. ADR-001 A1×B1 specialisation model spike-verified across all 12 DMBB features. 18 shared engines, dmbb = 44 forms/51 datalists/17 UV categories, 125/125 unit. Read this FIRST, then the files in §2.**

**LIST UX (QA1–QA3, datalist/userview only, no plugin change) — all closed:** every column sortable + typed filters + drill-downs (QA1); detail lists list_enfActions/list_instalments + 0 leaky labels (QA2); wide lists curated via a `listColumns` form hint (worst 18→7, key-led), the 8 batch-run/check triggers consolidated into one admin **"Operations — batch runs"** category (3 single-trigger categories retired), and `clean_label()` now strips all gloss parentheticals except a units/codes allowlist (QA3). **run_t23/24/25 all green.**

**DASHBOARD TIER (14 Jun — ADR-002, native, GREEN):** the interactive dashboard tier is now built the *solid* way per **ADR-002 (Accepted)** — native enterprise **`SqlChartMenu`** (server-side SQL → Apache ECharts) bound to the tested MI datalist binders, **no custom HTML/JS, no scraping, no bespoke plugin**. The **Dashboards** category (console landing) carries 4 charts; `gen_userview.py` gained a `sqlchart` emitter; **run_t26 5/5**. Two earlier custom-Chart.js interims (HtmlPage strips scripts; CustomHTML/FormMenu renders but scrapes datalist HTML) were rejected/retired (ADR-002 + DX9-DELTAS). Remaining increment: DashboardMenu one-page-per-tier (RPT-FR-006), K2 KPI traffic-light tiles via `mdCollectionParam` (RPT-FR-003), 3-tier role split (RPT-FR-007 — rides on the GroupPermission membership fix, task #60).

**REGRESSION STATE (14 Jun — FULLY GREEN, order-independent; now t02..t26 = 25/25):** the canonical sweep `scripts/run_regression.sh` runs **run_t02..t25 = 24/24 green**, proven with **two consecutive cold-start sweeps**. The four earlier in-sweep failures are all fixed: **t03/t08** (round-robin cursor + suppression races) now `drain()` running envelopes at suite start and assert round-robin alternation relatively, so they're order-independent; **t07** just needed `MAYAN_USER`/`MAYAN_PASSWORD` exported (the runner now sources the live autoadmin pw); **t09** was a real CMBB-F09 bug — the clickhouse-jdbc `-all` jar's Apache-HC5 path throws `NoClassDefFoundError: org/slf4j/LoggerFactory` and poisons the ClickHouse client JVM-wide, so both gateways (`JdbcOutcomeGateway`, `JdbcGoldGateway`) now pin `http_connection_provider=HTTP_URL_CONNECTION`. The runner cold-starts Tomcat first (RESTART=1) for a clean JVM; a residual same-warm-JVM t09 non-idempotency (2nd SHIP → WorkflowManagerImpl NPE) is logged as an open item but never hit because each sweep restarts. The mock ClickHouse/Mayan were healthy all along (the earlier "re-provision" hunch was wrong — see DX9-DELTAS 2026-06-14).

## 1. Where the project stands

CMBB (generic case management building block) on jdx9: **F01–F09 all through gate G4, Accepted — CMBB feature-COMPLETE.** F09 (events-kpi: WF-FR-020 full + I-1/I-2) is live: **72/72 unit tests**, **run_t09 8/8 PASS**, **full regression run_t02..run_t09 green** (8+7+6+7+7+5+8+8). Plugin bundle now has **9 engines** (added EventEmitter, OutcomeWriteback; GoldMartClient shared lib). App at **45 forms / 52 datalists / 10 UV categories**. F09 delivered: audit-chain VERIFY/tamper-evidence (ChainVerifyService), KPI emission (KPI_EMITTED dims), GoldMartClient JDBC read of `sta_v1` with INT-FR-004 degraded cache, OutcomeWriteback idempotent insert to ClickHouse `fact_case_outcomes` (queue-retry), SLA compliance % (DL-list_sla_compliance, deferred from F05). Embedded **clickhouse-jdbc 0.6.0-patch5**. **All 20 WF-FRs + F08 GCMF caps + F09 Accepted.**

**Deploy lessons baked in (don't relearn — see DX9-DELTAS rows dated 2026-06-12):** DEV cycle is **delete → import → publish → RESTART Tomcat → seed**. `scripts/deploy_jwa.py` now **discovers the real app version** (delete ALL prior versions, publish the version actually created) — the old hardcoded `/1/` delete+publish left NOTHING published once versions advanced → ApiBuilder.getJson NoSuchElementException → HTTP 400 on every form post (a restart does NOT fix that; publishing the right version does — `scripts/_publish_v.py`/`_delete_v.py` are the one-off helpers). A fresh import does NOT materialise new forms until the old app is deleted; reimported API defs return 400 until Tomcat restart (boot-singleton). `app_fd_*` data survives the delete. Plugin-property `value` defaults are UI-only — engines carry code-level DEV defaults for any connection config (the ClickHouse JDBC URL was empty otherwise). For run_t07 export `MAYAN_PASSWORD=$(docker exec mtca-mayan-pg psql -U mayan -d mayan -t -A -c "SELECT password FROM autoadmin_autoadminsingleton;")`. ClickHouse mock: `docker {stop,start} mtca-clickhouse`; Gold TINs are `\d{6}[MGHABL]` only.

**This session ran on the Mac via Desktop Commander** (mvn build, deploy, Tomcat restart, ClickHouse stop/start, all 8 acceptance suites + full regression) — author+generate still in Cowork. The split works either way.

**ARCHITECTURE DECIDED (ADR-001, A1×B1, SPIKE-001 verified):** modules are CMBB *specialisations*, not siblings — a module case = ONE `cmCase` spine row (caseType) extended via `subjectId` to a module-owned subject form + child grids; module logic = extension-point plugins in the shared `cmbb-plugins` bundle; **two Joget apps** (`cmbb` platform + `<module>` app, e.g. `dmbb`). Cross-app facts proven live: `app_fd_*` are global (a module form on `tableName: cmCase` unions additively, engines unaffected), process start by global id, each app needs its OWN `api_credential` key, seed CMBB-owned tables via `API-cmbb-data` / module-local via `API-<module>-data`. Read `docs/design/ADR-001-*.md` + `SPIKE-001-*.md` + the `joget-feature-loop` skill's "Module onboarding" section. CMBB amendment kept: `mmCaseType.subjectFormId`.

**DMBB slice S1 COMPLETE 2026-06-12 (F01+F02+F03).** F01: DPM carriers (mdDebtCat C1–C6 Gold-aligned, mdInstrument Malta-14 + 4 foreign disabled, mmStrategy/mmEscStep STD-MLT). F02: the **DM case type** — a debt case = `cmCase` spine row (caseRef DM-######) + `dmDebt` (1:1) + `dmLine` (1:many); `gen_forms` gained `SubForm`+`FormGrid` emitters. F03: **DebtIdentificationJob** — scans `sta_v1` via **GoldMartScanner**, consolidates per TIN (BR-DM-004), categorises from the Gold band (BR-DM-003), dedups (BR-DM-002), and **creates+starts** the envelope (the deferred CaseStarter — `JogetProcessStarter` + `WorkflowManager.processStart`). Bundle = **10 engines, 75/75 unit**; `dmbb` = 8 forms/8 datalists/3 UV categories. **run_t10/11/12 all green + CMBB run_t02..t09 green.**

**Reusable platform fact (F03):** a plugin can start the CMBB envelope cross-app — `appService.getPublishedAppDefinition("cmbb")` → `getWorkflowProcessForApp("cmbb",version,"cmCaseEnvelope")` → `wm.processStart(procId, null, {caseId,assignee}, null, caseId, false)`. Identification/auto-create tests must be **wipe-free** (SQL-DELETE on `app_fd_cmcase` poisons the Hibernate cache the dedup engine reads — assert end-state). The deferred **`CaseSubjectFormElement`** (per-type subject form on the in-workflow activity screen) is still pending — build when an officer first acts on debt content mid-lifecycle (F04+).

**DMBB-F04 DONE 2026-06-13.** **EscalationEngine** (11th engine) walks the F01 `mmStrategy`/`mmEscStep` ladder against each open DM case's elapsed time (category-gated BR-DM-006, time-travel `asOf`), advances `dmDebt.stage`/`lastStepSeq` (Reminder→Demand→Final demand→enforcement), and emits `NOTIF_PENDING(reason=template)` → the existing **F06 NotificationDispatcher** sends the notice (`cmNotif` SENT). Seeds: `mdTemplate` (TPL-REMINDER/DEMAND/FINAL/GARNISH/SEIZURE) + `mmNotifRule` (DM×5) via API-cmbb-data. **run_t13 4/4 + 79/79 unit + full regression green.** `dmbb` = 9 forms/9 datalists/4 UV categories. Test-isolation lessons: per-suite TINs that don't overlap Gold's identified set; scope cross-suite count checks to a suite's own data.

**DMBB-F05 DONE 2026-06-13 — slice S2 COMPLETE (F04+F05).** Enforcement trigger (BR-DM-030) folded into the EscalationEngine: an enforcement step fires only when no full-amount objection and no active ENFORCEMENT_SUPPRESS hold (ENFORCEMENT_TRIGGERED/BLOCKED). Strategy-admin consistency (DM-FR-015) = EscalationEngine VALIDATE mode (cmStrategyCheck → StrategyAdminService: enabled instruments, C2–C6 coverage, no overlapping windows). **run_t14 3/3 + 84/84 unit + full regression green.** `dmbb` = 10 forms/10 datalists/5 UV categories. No new engine.

**DMBB-F06 DONE 2026-06-13 — slice S3 started.** Instalment agreements (DM-FR-021…028, BR-DM-017…029): **ReliefProductInterpreter** (12th engine) wrapping a pure **ReliefService**, two modes. APPLY (`dmInstAgr` create, debtCaseId→cmCase): eligibility (mdRelief categories C3–C5, single-active BR-DM-018, min instalment BR-DM-020) → `dmInstLine` schedule with projected reduced-rate interest (mdProjRate, D-SAD-04 informational) → auto-approve within params (BR-DM-021) else UNDER_REVIEW; on ACTIVE it asserts an **ENFORCEMENT_SUPPRESS hold** on the debt case. COMPLIANCE (`cmInstComplianceRun`): missed-line detection within grace (mdViolation), ≥ threshold consecutive misses → auto-CANCEL + release hold + open recovery DM case (F03 ProcessStarter, triggerOrigin INSTALMENT_DEFAULT). **The active-plan hold is the seam designed forward in F05 A2 — T-15.2 proves it live: an active instalment makes the F05 gate emit ENFORCEMENT_BLOCKED.** Chose the IA-as-standalone-form model (a debt case carries a history of plans), NOT a new CMBB case type. **run_t15 4/4 + 89/89 unit (+5 ReliefServiceTest) + full regression run_t02..t14 green.** `dmbb` = 16 forms/16 datalists/7 UV categories (+dmInstAgr/dmInstLine/cmInstComplianceRun + mdRelief/mdViolation/mdProjRate). New DX9-DELTA: a new MD carrier must ship its `F-<md>.spec.yml` form (not just the seed CSV) — the seed 400s with "relation does not exist" until the table is created by deploying the form.

**DMBB-F07 DONE 2026-06-13 — slice S4 started.** Enforcement actions (DM-FR-029…038, BR-DM-031…035/044…046), **full breadth, config-driven**: **EnforcementActionEngine** (13th engine) + **EnforcementActionService** + **GarnishingConnector** (bank-WS stub). All 14 Malta instruments execute through ONE `dmAction` record branching on `mdInstrument.executionMode` (ADMINISTRATIVE incl. garnish/publish/lien/third-party; JUDICIAL court submission; FIELD seizure/agent). Modes: INITIATE (proportional gate BR-DM-031 = mdInstrument.minCategory+proportionalityMinAmount; F05 eligibility re-check; legal-fee `dmCharge` DM-FR-038; garnish cap BR-DM-033/035 recovered writeback), AGENT_APPOINT (`dmAgent`, C4–C5 BR-DM-044, mdAgentFee), AGENT_REPORT (`dmAgentRpt`, commission = recovered×rate BR-DM-045), SWEEP (`cmEnfActionRun`: PUBLISH/RELEASE debtor list DM-FR-030, AGENT_ALERT BR-DM-046, CONFIRM). **run_t16 8/8 + 97/97 unit (+8 EnforcementActionServiceTest) + full regression run_t02..t15 green.** `dmbb` = 25 forms/25 datalists/9 UV categories (+dmAction/dmAgent/dmAgentRpt/dmCharge/dmDebtorPub/cmEnfActionRun + mdLegalFee/mdAgentFee/mdPublishRule). Deferred + recorded: live external transport for every connector (bank WS, courts, asset/debt registries) is a deployment binding; auction modelled to SOLD; legal-fee/recovery *postings* external (SAD I-4, DMBB records the hand-off). Test-fixture note: cmHold requires `basis`+`caseRef` on the API path.

**DMBB-F08 DONE 2026-06-13 — slice S4 COMPLETE (F07+F08).** Enforcement-config console (DM-FR-039/040/041): **EnforcementConfigEngine** (14th engine) + **EnforcementConfigService**, two modes. VALIDATE (`cmEnfConfigCheck`, DM-FR-039 "testable before production") consistency-gates every enabled mdInstrument — required attributes, docTemplate resolves to an active mdTemplate, costRecorded has an mdLegalFee; returns valid/issueCount/issues. PREVIEW (`cmTemplatePreview`, DM-FR-040+041) renders an mdTemplate against a case's data with merge fields, honouring `mdNoticeRule.consolidationMode` (SEPARATE = one notice per dmLine taxType; CONSOLIDATED_ALL = one notice, summed). Extended mdInstrument with the 3 missing DM-FR-039 attributes; completed the enforcement-document template library (7 missing TPL-* + de-truncated the 5 F04 templates). **run_t17 4/4 + 104/104 unit (+7 EnforcementConfigServiceTest) + full regression run_t02..t16 green.** `dmbb` = 28 forms/28 datalists/10 UV categories. Two findings (DX9-DELTAS): seed CSV values with commas must be quoted (F04 mdTemplate bodies were silently truncated — surfaced by F08 preview, now fixed); run_t10 T-10.2 instrument count scoped to countryProfile IN (MLT,FOREIGN) so a tagged TEST instrument can't break it.

**DMBB-F09 DONE 2026-06-13 — slice S5 started.** Write-off (DM-FR-042…046, BR-DM-036…039): **WriteOffEngine** (15th engine) + **WriteOffService**, three modes. SUBMIT (`dmWriteOff` create): evidence guard (BR-DM-039 — an APPROVED request needs history+evidence+rationale else REJECTED) + delegation routing (BR-DM-037 — DMO<1000/SDO<20000/DIRECTOR from mdWoDelegation); a C1_AUTO request posts immediately. APPROVE (`cmWriteOffApprove`): an officer's decision posts or rejects (DM-FR-043). SWEEP (`cmWriteOffRun`): AUTO_C1 (DM-FR-042/BR-DM-036 — auto-write-off + close every C1 case, no human intervention), STATUTORY_BULK (DM-FR-044/BR-DM-038 — debts aged ≥ mdWoPolicy.statutoryYears from firstAssessedDate), C2_PASSIVE (DM-FR-045 — mark C2 passive-collection). Posting closes the case + stamps `dmDebt.writeOffStatus`, preserving the case + cmEvent chain (DM-FR-046); the account posting is external (SAD I-4, recorded as a dmCharge WRITE_OFF). Modelled as a debt-case **outcome record** (F06/F07 pattern), not a parallel case. **run_t18 7/7 + 112/112 unit (+8 WriteOffServiceTest) + full regression run_t02..t17 green.** `dmbb` = 34 forms/34 datalists/12 UV categories (+dmWriteOff/cmWriteOffApprove/cmWriteOffRun + mdWoGround/mdWoDelegation/mdWoPolicy; dmDebt extended). list_dmWriteOff is the Write-Off Report surface (RPT-FR-012; Jasper deferred). No new platform delta.

**DMBB-F10 DONE 2026-06-13 — slice S5 (F09+F10 done).** Default-assessment tracking (DM-FR-054…056, BR-DM-051…053): **DefaultAssessmentEngine** (16th engine) + **DefaultAssessmentService**, three modes. ASSESS (`dmDefAssess` create): auto-estimate by priority (prior-year ?? industry average ?? fixed, mdEstMethod, BR-DM-051) + reasonableness check (estimate > prior×mdDefAssessPolicy.reasonablenessMultiplier 1.5 without justification → NEEDS_JUSTIFICATION, BR-DM-052) + informational debit. REPLACE (`cmReturnFiled`): a filed return reverses the default, records variance, emits RISK_PROFILE_UPDATE (DM-FR-055/BR-DM-053). ESCALATE (`cmDefAssessRun`): ASSESSED + no filing past nonFilingGraceDays → a DM debt case **banded to its applicable category** (mdDebtCat) + started via the F03 ProcessStarter (DM-FR-056). **run_t19 5/5 + 119/119 unit (+7 DefaultAssessmentServiceTest) + full regression run_t02..t18 green.** `dmbb` = 39 forms/39 datalists/14 UV categories (+dmDefAssess/cmReturnFiled/cmDefAssessRun + mdEstMethod/mdDefAssessPolicy). No new platform delta.

**DMBB-F11 DONE 2026-06-13 — slice S5 (F09+F10+F11 done).** Debtors list (DM-FR-057/058): **DM-FR-057** = the **list_debtorsList JdbcDataListBinder** (first DMBB custom JDBC datalist — join cmCase+dmDebt, columns TIN/taxpayer/total-debt/age/category/stage/risk/assignee/tax-type, combinable filters, native CSV/Excel; excludes written-off + terminal). **DM-FR-058** = **DebtorsListEngine** (17th engine) EXTRACT: builds the national-registry extract (count/total/CSV-or-JSON payload, DEBTORS_EXTRACT_GENERATED) from F07's PUBLISHED dmDebtorPub set, excluding resolved — reuses F07's publish/RELEASE machinery (no duplication; the website-publish lifecycle was already F07's). **run_t20 4/4 + 122/122 unit (+3 DebtorsListServiceTest) + full regression run_t02..t19 green.** `dmbb` = 40 forms/41 datalists/15 UV categories (+dmDebtorsExtract + list_debtorsList). Acceptance technique: run_t20 reads the deployed datalist's binder SQL from app_datalist.json and executes it (tests the real query). No new platform delta.

**DMBB-F12 DONE 2026-06-13 — DMBB MODULE COMPLETE (F01–F12).** Collection MI (DM-FR-047…053): **CollectionMiEngine** (18th engine) + **CollectionMiService**, dispatched by cmCollectionMiRun.runMode — ROLLUP (plan-vs-actual: actual = Σ dmAction.recoveredAmount + Σ dmAgent.recoveredTotal per dmPlanTarget category + attainment %, DM-FR-047), SUGGEST (target = current category stock × mdCollectionParam.recoveryRate, BR-DM-043), RECONCILE (②/③ writeback completeness: every WRITEOFF_POSTED event has its dmCharge WRITE_OFF, CAD §132). DM-FR-048 reports = 4 MI JDBC datalists (list_debtByCategory/debtAging/recoveryByAction/instalmentCompliance). DM-FR-049/050/051/052/053 were already delivered (CMBB-F09 GoldMartClient / cmEvent chain / CMBB-F06 notifications / CMBB-F07 Mayan / CMBB-F08+DMBB-F09 closure) — cross-referenced in the FIS/TRACE for 100% coverage. **run_t21 4/4 + 125/125 unit (+3 CollectionMiServiceTest) + full regression run_t02..t20 green.** `dmbb` = 44 forms/49 datalists/17 UV categories.

**NO DMBB WORK REMAINS — the module is feature-complete + the Jasper report tier is shipped.** Out-of-DMBB program items (not blocking; pick up when prioritised): (1) ✅ **DONE — Jasper RPT track**: 6 tabular reports (Debt Aging / Tax Debt Status / Recovery by Action / Instalment Compliance / Write-Off / Debtors List) ship as JasperReportsMenu (Enterprise) over the F09/F11/F12 datalist SQL, PDF+Excel, **run_t22 6/6 PASS + regression green**; gen_userview gained a `jasper` emitter (config-as-code). ✅ **DONE — list UX/QA remediation (DMBB-UX-QA)**: a user QA review caught that the lists lacked sorting, real filters and drill-down (and that earlier FIS/TRACE had over-claimed those) + an orphan "Debt lines" menu. Fixed at the generators: every column sortable, typed filters (SelectBox + native contains; auto TIN/status on CRUD lists), HyperlinkDataListAction drill — summary→detail (`Debt by category` cell → `list_debtorsList?fcat=<cat>`) and detail→record (debtors "Open case" → case); JDBC filter/drill via `#requestParam.X#` SQL guards; orphan menu removed. **run_t23 5/5 (incl. functional `?fcat=C5` drill) + full regression run_t02..t22 green.** Two generator extensions recorded in DX9-DELTAS. ✅ **+ UX-QA round 2**: added two JDBC detail lists (list_enfActions, list_instalments) so Recovery-by-action / Instalment-compliance / Debt-aging summaries drill to them; added a TIN filter on the debtors list; and **cleaned 61 implementation-leak column labels** ("(set by engine)", "(FK)", "[DM-FR-039]") via gen_datalists.clean_label() → 0. **run_t24 5/5 + full regression run_t02..t23 green.** Known limitation (DX9-DELTAS): a JDBC list honours only ONE `#requestParam` filter per URL — single-param drills (all we emit) work; multi-criteria filter on a JDBC list needs a different approach. dmbb now 44 forms / 51 datalists / 19 UV categories. REMAINING RPT sub-item = the **interactive dashboard tier** (RPT-FR-003 KPI traffic-lights/sparklines, RPT-FR-006/007 multi-stream + role hierarchy, drill-down, <10s) — a BI front-end Jasper does poorly; out of scope of the tabular track. (2) **Live connector bindings** at deployment — bank WS (DM-FR-029), courts (DM-FR-033/035), asset/debt registries (DM-FR-031/058), national debtors registry — replace the recorded DEV stubs with real transport. (3) **Full Gold ②/③ value reconciliation** against ClickHouse `fact_case_outcomes` (CMBB-F09 OutcomeWriteback) — F12 ships the completeness framework. (4) **ASA Semantic Mapping Spec** — remains BLOCKED on legacy DB access (Gordon). (5) Other modules per the program (AMBB/APBB/RFBB/… on the same ADR-001 fabric).

## 1a. Skills — use them, they carry the method

The **`joget-feature-loop`** skill is the conductor for ALL feature work: invoke it
first for anything like "continue with F09", "build the next feature", "redeploy",
"run acceptance", "the import failed", "start DMBB". It owns the per-feature loop,
the 21 platform deltas (with mechanisms) and the test-runner anatomy — §4/§5 below
are only a condensed fallback of what it covers in depth.

Route the specialist work to the specialist skills (the loop skill defers to them):

| Task | Skill |
|---|---|
| Any pipeline stage, deploy, acceptance, regression | **joget-feature-loop** (always first) |
| FIS authoring, traceability/AC analysis, acceptance-test design | joget-req-analyst (Output F/G formats) |
| Java engine code, FormDataDao queries, OSGi/pom, JAR deploy | joget-plugin-dev (read its references/ FIRST) |
| Form / datalist / userview / workflow JSON schemas | joget-form-gen / joget-datalist-gen / joget-userview-gen / joget-workflow-gen |
| "what's stored?", acceptance SQL, app_fd_ conventions | joget-db-inspect |
| New/changed instances, ports, instances.yaml | joget-instance-setup |
| Pixel-perfect letters/reports (DMBB notices) | joget-jasper-report |
| F09 ClickHouse work (sta_v1 reads, writeback) | arms-dev-workflow conventions (JDBC, invoker-rights views, `</dev/null` on INSERT) |

Caveats learned this session: the gen skills are DX8-era — where they conflict with
`docs/DX9-DELTAS.md` or the repo's `scripts/gen_*.py`, the repo wins (generators are
the executable truth). joget-workflow-gen's references/scripts are missing from the
cache — XPDL/map shapes live in `scripts/gen_workflow.py` and the feature-loop skill.
joget-deploy's import mechanics predate DX9 — use `scripts/deploy_jwa.py` + the
feature-loop skill's Stage 4 instead.

## 2. Read these files before doing anything
| File | Why |
|---|---|
| `docs/JOGET-DELIVERY-WORKFLOW.md` (in 13_Case_Management area, see master plan) | The staged method: Scope→CAD→FIS→generate→deploy, gates G0–G4 |
| `cmbb/TRACE.md` | FR-by-FR status ledger + per-feature G4 close-out notes (the session history in compressed form) |
| `docs/DX9-DELTAS.md` | ~21 platform gotchas, EVERY ONE learned the hard way. Violating these costs hours |
| `cmbb/CAD-CMBB.md` §7 | Feature decomposition; F08/F09 rows define the next scope |
| `docs/design/Plugin-Dev-Baseline.md` | Plugin template decisions (GAM pattern, wflow-core 8.1-SNAPSHOT) |
| `docs/ops/ENVIRONMENT.md`, `docs/ops/DEPLOYMENT.md` | Instances, credentials, runbooks |
| An existing FIS folder, e.g. `cmbb/features/CMBB-F05-deadline-sla/` | The exact format every feature follows (FIS.md, forms/, plugins/, tests/run_tNN.py) |

## 3. Environment (all on this Mac, all currently running)
- **jdx9**: Joget 9.0.7 Enterprise, `/Users/aarelaponin/joget-enterprise-linux-9.0.7-9`, http://localhost:8089/jw, admin/admin. DB `jwdb_mtca` on pg1 (joget_mtca/joget_mtca). Start: `./tomcat.sh start` (JDK 21 pinned; `tomcat.sh restart` does NOT work — kill PID then start, verify a NEW "Server startup in" log line).
- **Mock platform**: `platform-mock/` docker (ClickHouse :8123/:9000 user mtca; Mayan EDMS :8880 admin/<see below>).
- **Mayan password**: autoadmin pw, exported as MAYAN_URL/MAYAN_USER/MAYAN_PASSWORD inside `tomcat.sh` (already patched); retrieve: `docker exec mtca-mayan-pg psql -U mayan -d mayan -c "SELECT password FROM autoadmin_autoadminsingleton;"` (also currently in /tmp/mayan_pw — tmp, may vanish).
- **Plugin build**: `cd cmbb/plugins/cmbb-plugins && mvn clean package` (host has mvn + ~/.m2 with wflow-core 8.1-SNAPSHOT). Deploy JAR: `cp target/*.jar <jdx9>/wflow/app_plugins/` — hot-reloads in ~10s ("Installed plugin" in catalina.out).
- **Deployment toolkit venv**: `/Users/aarelaponin/PycharmProjects/dev/joget-deployment-toolkit/.venv/bin/python`, instance registry `~/.joget/instances.yaml`, env `JDX9_PASSWORD=admin`.

## 4. The proven per-feature loop (condensed — `joget-feature-loop` skill is the authoritative version)
1. **Stage 2**: write `cmbb/features/CMBB-Fnn-<name>/FIS.md` (+ forms/*.spec.yml, plugins/PL-*.md, tests/acceptance.md) — copy structure from F05/F06/F07. FR text verbatim from `docs/reqs/08-Debt_Management-Requirements_v1.1.md`.
2. **Stage 3 generate**: `python3 scripts/gen_forms.py <feature>/forms cmbb/generated/forms` · `gen_datalists.py` (form specs → companions; custom DL specs with jdbc binder supported) · `gen_userview.py <ALL seven UV-delta files in feature order> cmbb/generated/userviews cmbb/generated/forms cmbb/generated/datalists` · `gen_workflow.py` only if WF spec changed · `build_jwa.py cmbb/generated cmbb "CMBB Case Fabric" cmbb/generated/cmbb.jwa`.
3. **Stage 3 plugin**: extend the bundle (new engine class + service class + Activator registration + properties JSON + tests on GuardTestHarness), `mvn clean package`, copy JAR.
4. **Stage 4 deploy**: delete app (POST `/web/console/app/cmbb/1/delete` with OWASP_CSRFTOKEN from `/csrf`+Referer) → `scripts/deploy_jwa.py jdx9 cmbb/generated cmbb "CMBB Case Fabric"` → seeds via `scripts/load_md_seed.py http://localhost:8089/jw API-cmbb-data <key> <seed_dir>` (key: `SELECT apikey FROM api_credential WHERE apiname='cmbb-dev-key'`). app_fd_* data SURVIVES app deletion.
5. **Stage 4 test**: write `tests/run_tNN.py` modeled on run_t07.py, run it, then ALWAYS full regression `for t in 02..NN`. Update FIS Status + TRACE.md rows + DX9-DELTAS for any new finding.

## 5. Critical conventions (condensed — full mechanisms in the skill's references/platform-deltas.md and docs/DX9-DELTAS.md)
- Never hand-edit `cmbb/generated/` — fix the spec or generator, regenerate (P5).
- Never write app_fd_* outside the Joget API (P3); raw SQL only in JdbcDataListBinder SQL and read-only test assertions; test fixture wipes of cm* rows are the documented exception.
- Engine triggers: form postProcessor (`runOn: create` — the only proven value) for form-driven actions; MultiTools `tools[]` chains in guardOpen/guardFinal for lifecycle steps. NO new XPDL ever (envelope is the only process). NO PluginWebSupport while compiling against 8.1 (jakarta/javax mismatch).
- Plugins must `dao.updateSchema(form, table, new FormRowSet())` before first READ of any owned table (missing relation poisons the JTA tx; try/catch cannot recover).
- All /web/json/* calls need `Referer: <base>/web/console/home` + OWASP_CSRFTOKEN on POSTs; assignment list needs `rows=1000`.
- Test runners: per-run unique record ids; deactivate officer pool when the flow must stay in admin's inbox (see run_t02 set_officer_pool); check form API response BODY for non-empty `errors` (HTTP 200 ≠ success).
- cmEvent chain: ordered by zero-padded `seq` (writer-managed). Verify chains ORDER BY c_seq.
- Joget org roles in participant maps = `type: group` + dir_group rows (NOT type role).

## 6. Open ledger (don't lose these)
- OPEN: userview theme/permission + per-role landing page; alert unread badge widget; TA-RDM L2 YAML location (OPEN-1); retention purges (90d alerts / 7y notif log) as ops jobs; Jasper pixel-perfect letters at DMBB notices; postal/SMS/SMTP gateway config at deployment; consolidation_mode (E3) at DMBB notices; assignment-inbox hygiene (abort stale DEV processes someday).
- Pending task: A2 from Gordon call — share the Claude→Joget pipeline with Gordon before/at Thursday's demo. The demo story (lifecycle→allocation→queues→SLA→notifications→documents) already works end-to-end on jdx9.

## 7. Suggested opening prompt for the new session

> Resume the MTCA debt management build in `/Users/aarelaponin/IdeaProjects/rsr/mt-tca-prj/15-ITCAS/03_debt_management`. Read `docs/SESSION-RESTART-NOTE.md` first. Then invoke the **joget-feature-loop** skill and follow it to deliver CMBB-F09-events-kpi end to end (Stage 2 FIS → generate → plugin build → deploy → run_t09 + full regression run_t02..t09 → TRACE/DX9-DELTAS close-out). Use joget-req-analyst for the FIS, joget-plugin-dev for the Java, joget-db-inspect for SQL checks. Where any skill conflicts with docs/DX9-DELTAS.md or the repo's scripts/, the repo wins.
